# timkc-site

Personal portfolio site — plain HTML/CSS with a little vanilla JS. No build step required.

## Structure

```
public/   ← all site files
```

## Deploy

Push to `main`. GitHub Actions publishes to GitHub Pages automatically.
